# 数据加载

先用 `sel()` 或元数据探索锁定字段，再调用 `first()`、`all()` 或 `to_xarray()` 读取。
通用查询和输出约定在本分区集中说明；格式页只说明各格式的特有能力和限制。

```{toctree}
:maxdepth: 1

new-api
exploring-data
xarray-output
grib
grads
netcdf
table
legacy-api
```
