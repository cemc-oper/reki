# 数据查找

从数据的位置开始：新代码使用 `from_source()` 建立 source；需要复用逻辑数据集时使用
catalog。旧 `data_finder` 入口仍可用于维护现有业务配置。

```{toctree}
:maxdepth: 1

new-api
local-files
catalogs-and-patterns
remote-sources
troubleshooting
source-options
legacy-api
```
