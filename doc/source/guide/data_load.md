---
orphan: true
---

# 兼容入口：数据加载

此 URL 为兼容入口。GRIB2 的探索、选择、参数、层次、时效、metadata 和 xarray 输出请从
{doc}`/guide/grib/index` 进入。GrADS、NetCDF 和表格的格式限制仍集中在
{doc}`/guide/loading/index`；它们尚未提供与 GRIB2 相同的固定 E1 数据资产。

旧 `reki.format` 代码请使用 {doc}`/guide/loading/legacy-api`，迁移差异见
{doc}`/guide/migration`。
