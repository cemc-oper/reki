# Kept for Read the Docs, whose configuration installs a requirements file.
# The canonical local/CI declaration is the ``dependency-groups.docs`` group
# in pyproject.toml; keep this list synchronized with that group.
sphinx>=7.3
pydata-sphinx-theme>=0.15
myst-nb>=1.1
ipykernel>=6.29
codespell>=2.4
# Required by executable examples that use xarray interpolation.
scipy
