name: ci-build

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4
      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
          python-version: ${{ matrix.python }}
      - name: Sync test environment
        # The local sources are available in the development workspace, but
        # not in this standalone GitHub checkout. Use published metadata in CI.
        run: uv sync --group test --no-sources
      - name: Run tests
        # The regular data-backed tests use the rolling CMA-GFS WIS source.
        # Frozen real-data coverage is provided by the docs job below through
        # cedarkit-test-data's ecmwf_ifs assets.
        run: >-
          uv run --no-sources pytest tests -v --tb=short
          -m "not needs_data and not cma_hpc and not cmadaas_local and not cmadaas_service"
      - name: Build
        run: uv build --no-sources

  docs:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
          python-version: "3.12"
      - name: Install dependencies
        run: |
            uv pip install -r doc/requirements.txt
            uv pip install .
      - name: Build and check docs
        run: uv run --no-sync make -C doc check SPHINXOPTS="-W --keep-going"
